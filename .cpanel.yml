---
deployment:
  tasks:
    # Install dependencies
    - npm install
    # Build the SvelteKit app
    - npm run build
    # Tell Passenger (cPanel's Node server) to restart the app
    - mkdir -p tmp
    - touch tmp/restart.txt




    
